<%=form_with model: @review, method: 'POST' do |f| %>
    <%= f.hidden_field :reviewee_id, value: @user.id %>
    <%= f.label :body, "Body" %>
    <%= f.text_area :body %><br>
    <%= f.submit "Review!"%>

<% end %>
